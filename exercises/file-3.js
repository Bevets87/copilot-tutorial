// exercises/file-3.js

// This file contains a function that processes user data

function processUserData(userData) {
  return `User ${userData.name} is ${userData.age} years old.`;
}

module.exports = processUserData;
